require 'spec_helper'

describe HelpingHand do
  it 'has a version number' do
    expect(HelpingHand::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
